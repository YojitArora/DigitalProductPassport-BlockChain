const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("PassportRegistry - Manufacturer Tests", function () {
  // Test suite ready for manufacturer logic implementation
});
